This folder contains `Zustand` state stores. The `Zustand` package is used in this app as an advanced and
boilerplate-free alternative to react's Context component. Zustand's `persist` facility will be used in the future to
persist states in order to avoid data loss on browser refresh.
